# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.cloud.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/kernel/webserver.pid;
error_log /var/log/sdkwork/kernel/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    upstream gateway {
        least_conn;
        keepalive 32;
        server sdkwork-api-cloud-gateway:8080;
    }
    server {
        listen 80;
        server_name kernel-demo.sdkwork.com kernel-demo.birdcoder.com kernel-demo.dtupay.com kernel-demo.noaper.com kernel-demo.sdkwork.cn kernel-demo.birdcoder.cn kernel-demo.dtupay.cn kernel-demo.noaper.cn kernel-demo.skubc.com kernel-demo.skubc.cn kernel-demo.zowalk.com kernel-demo.zowalk.cn kernel-demo.offer86.com kernel-demo.offer86.cn kernel-demo.86offer.com kernel-demo.86offer.cn;
        include snippets/gateway-api-locations.nonproduction.conf;
        location / {
            root /usr/share/sdkwork/kernel/web/static;
            index index.html;
            try_files $uri $uri/ =404;
        }
    }
}
